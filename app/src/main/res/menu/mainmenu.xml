<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/filter"
        android:icon="@mipmap/filter"
        android:title="action_plus"
        app:showAsAction="ifRoom">
        <menu>
            <group android:id="@+id/filter_group"
                android:checkableBehavior="single">
                <item android:id="@+id/filter_bl"
                    android:title="Mostrar BL's"/>
                <item android:id="@+id/filter_inventario"
                    android:title="Mostrar Inventario"/>
            </group>
        </menu>
    </item>
</menu>